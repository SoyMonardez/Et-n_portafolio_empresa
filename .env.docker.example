PGDATABASE=etan
PGUSER=etan
PGPASSWORD=cambiar_esto

# Generar uno propio en el deploy real: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=cambiar_esto_por_un_secreto_largo_y_aleatorio
